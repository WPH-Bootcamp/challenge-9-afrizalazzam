import { SiteLayout } from "@/components/shared/site-layout";
import { AddressContent } from "@/features/address/address-content";

export default function DeliveryAddressPage() {
  return (
    <SiteLayout>
      <AddressContent />
    </SiteLayout>
  );
}
